# Internal navigation

Root: `/internal`. Shell: `src/app/internal/layout.tsx` + `OpsShell`. Nav config: `src/lib/platform/navigation.ts`. Labs preserved under Labs section. Public sitemaps and llms files exclude `/internal`.

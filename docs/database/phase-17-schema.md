# Phase 17 schema

Migration `20260731000000_phase17_platform_operations.sql` adds platform ops tables with RLS enabled and no customer policies (service role after Clerk + platform permission checks). See engineering docs for table purposes.

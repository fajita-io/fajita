# Blog content model

Articles use `articleFrontmatterSchema` in `src/lib/content/schema.ts` plus `ContentBlock[]` bodies. Required for publish: thesis, originalContribution, review flags, author, dates, related links. See `defineArticle` in `src/lib/content/types.ts`.

# Free tools architecture

Tools are registry entries plus client engines under `src/lib/content/tools/engines/`. Launch set is client-side only. Networked HTTP checker deferred. Privacy statements must describe exact behavior.

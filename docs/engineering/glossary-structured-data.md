# Glossary structured data

Internal. Term pages emit `DefinedTerm` + `BreadcrumbList` (+ `FAQPage` when FAQs exist).
Index emits `DefinedTermSet`. Publisher is Fajita. Wiki is attribution, not publisher.

# Glossary term registry

Internal. `src/lib/glossary/registry.ts` aggregates content modules, enforces uniqueness,
strips broken related links in development, validates redirects, and exposes lookups for
navigation, sitemap, search, and AI files.

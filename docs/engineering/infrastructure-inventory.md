# Infrastructure inventory

Tables: `platform_infrastructure_services`, `platform_environment_inventory`, `platform_configuration_settings`, `platform_releases`. Secret values never stored; reference names only. No deploy/migrate triggers from the UI.

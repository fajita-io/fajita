# Payback model

`CAC ÷ expected monthly contribution per retained organization`

Contribution uses collected revenue minus fees, compute, DB, alerts, subscriber email, support, affiliate commission. Never gross MRR. Always labeled estimate.

# Platform approvals

Table: `platform_approvals`. States separate approval from execution. High-risk self-approval blocked when multiple roles exist; solo founder uses step-up + cooling-off. Expired approvals cannot execute.

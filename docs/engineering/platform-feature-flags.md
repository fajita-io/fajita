# Platform feature flags

Code registry remains source of truth (`FEATURE_REGISTRY`). Org overrides in `feature_flag_overrides`. Change history in `platform_feature_flag_changes`. Production rollouts require approval + step-up.

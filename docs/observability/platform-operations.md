# Platform operations observability

Monitor analytics job lag, reconciliation failures, approval backlog, provider health, worker heartbeats, and export queue. Structured logs via `logPlatformAdminAction`. No secrets in logs.

# Phase 14 data map

Collected: page analytics (aggregate), redacted search queries (no-result table),
anonymous feedback, editorial audit via git. No visitor identity by default.
Retention: prune raw queries quickly; keep published term versions in git.

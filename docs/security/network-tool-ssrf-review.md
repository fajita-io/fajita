# Network tool SSRF review

HTTP status checker deferred. Monitor destination validation exists in `src/lib/monitoring/destination.ts` for product paths. Public one-shot fetch needs separate capacity and abuse controls before enablement.

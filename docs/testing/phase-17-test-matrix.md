# Phase 17 test matrix

Covered in vitest: permissions, metrics, dates, export sanitization, nav/palette, fixtures, event registry.

Manual / staging: command center states, monitoring drain, approvals lifecycle, revenue reconcile, security/privacy queues, reports/exports, mobile emergency access, reduced motion, 200% zoom.

Load tests: document results in `phase-17-load-results.md` after staging runs.

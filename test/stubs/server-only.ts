// Test stub for Next.js `server-only`. In production this module throws when
// imported into a client bundle; in unit tests we replace it with a no-op so
// pure server-side logic can be exercised in isolation.
export {};
